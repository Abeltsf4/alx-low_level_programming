#include <stdio.h>
/**
 * main - a c program that prints output using a printf function
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
